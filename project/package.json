{
  "name": "sejaelevar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:open": "vite --host 127.0.0.1 --open",
    "build": "tsc -b && vite build",
    "build:single": "npm run build && node scripts/build-single-file.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "preview:open": "vite preview --host 127.0.0.1 --open",
    "start": "node scripts/serve-preview.mjs --open",
    "serve": "node scripts/serve-preview.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.0.0",
    "vite": "^7.0.0"
  }
}
